# Platform to conduct test series

## Set Up
- Run `python manage.py runserver` . Server will start
- If it is first time, create migrations first:
  - `python manage.py makemigrations`
  - `python manage.py migrate`
